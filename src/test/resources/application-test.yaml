spring:
  jpa:
    show-sql: true
    generate-ddl: false
  sql:
    init:
      mode: never
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testReferenceProject;DB_CLOSE_DELAY=-1;MODE=MSSQLServer
    username: sa
    password: sa